# Simple, working Dockerfile for Creation Service
FROM node:20-slim

# Create app directory
WORKDIR /app

# Copy package.json
COPY package.json ./

# Install production dependencies
RUN npm install --production

# Copy application code
COPY . .

# Expose port
EXPOSE 8080

# Start the application
CMD ["node", "src/index.js"]